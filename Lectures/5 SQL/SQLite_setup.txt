#Install touch with node.js
npm install touch-cli -g

#creates file
touch flights.sql

#download sqlite and add to environment variables. then restart vscode

#Enter the sqlite to start writing commands
sqlite3 flights.sql

CREATE TABLE flights(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    origin TEXT NOT NULL,
    destination TEXT NOT NULL,
    duration INTEGER NOT NULL
);

#show tables
.tables

INSERT INTO flights (origin, destination, duration) VALUES ('New York', 'London', 415);

SELECT * FROM flights;


INSERT INTO flights (origin, destination, duration) VALUES ('Shanghai', 'Paris', 760);
INSERT INTO flights (origin, destination, duration) VALUES ('Istanbul', 'Takyo', 700);
INSERT INTO flights (origin, destination, duration) VALUES ('New York', 'Paris', 435);
INSERT INTO flights (origin, destination, duration) VALUES ('Moscow', 'Paris', 245);
INSERT INTO flights (origin, destination, duration) VALUES ('Lima', 'New York', 455);

#Improve formating
.mode columns
.headers yes

SELECT * FROM flights WHERE origin = 'New York';

SELECT * FROM flights WHERE duration > 500 OR destination = 'Paris';

SELECT * FROM flights WHERE origin LIKE '%a%';

UPDATE flights
    SET duration = 430
    WHERE origin = 'New York'
    AND destination = 'London';

DELETE FROM flights WHERE destination = 'San Jose';


INSERT INTO airports (airport, code) VALUES ('New York', 'JFK');

SELECT first, origin, destination
FROM flights JOIN passengers
ON passengers.flight_id = flight.id

CREATE INDEX name_index ON passengers (last);

#exit
.quit